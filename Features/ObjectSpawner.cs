using AdminToys;
using ProjectMER.Features.Objects;
using ProjectMER.Features.Serializable;
using UnityEngine;

namespace ProjectMER.Features;

public static class ObjectSpawner
{
	/*
	public static PrimitiveObject SpawnPrimitive(SerializablePrimitive serializablePrimitive)
	{
		PrimitiveObjectToy primitive = GameObject.Instantiate(PrefabManager.PrimitiveObjectPrefab);
		return primitive.gameObject.AddComponent<PrimitiveObject>().Init(serializablePrimitive);
	}
	*/
}
